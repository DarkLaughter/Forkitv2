<%= form_for @comment do |f| %>

<%= f.label :content, 'Comment' %>
<%= f.text_area :content %>


<%= f.hidden_field :meal_id, :value => @comment.meal_id %>
<%= f.hidden_field :user_id, :value => session[:user_id] %>
    
<% end %>